/**
 * Main Application Component
 *
 * This is the root component of the ROI Calculator App.
 * It serves as the entry point and renders the main ROI calculator interface.
 *
 * @component
 * @returns {JSX.Element} The application wrapper containing the ROI calculator
 */

import ROICalculatorApp from './ROICalculatorApp.jsx'

function App() {
  return <ROICalculatorApp />
}

export default App
